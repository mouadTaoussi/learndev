- Users (developers) can create account whether with github or local account
- Users (developers) can add a topic and add resourses to that topic
- Topics are under the controle by the app's owner
- Topics are contains (docs- courses- articles- projects ideas)
- there are career paths for beginners


How i tricked my brain for doing hard thins
https://www.youtube.com/watch?v=9QiE-M1LrZk

#0652DD primary color
#0984e3 primary color
#de078e secondary color

// Watch that leater
https://www.youtube.com/watch?v=33OAwHB5xOc

function paginate(model) {
  return async (req, res, next) => {
    const page = parseInt(req.query.page);
    const limit = parseInt(req.query.limit);
    const startIndex = (page - 1) * limit;
    const endIndex = page * limit;

    const result = {};

    if (endIndex < (await model.countDocuments().exec())) {
      result.next = {
        page: page + 1,
        limit: limit,
      };
    }
    if (startIndex > 0) {
      result.previous = {
        page: page - 1,
        limit: limit,
      };
    }
    try {
      result.results = await model.find().limit(limit).skip(startIndex);
      res.paginatedResult = result;
      next();
    } catch (e) {
      res.status(500).json({ message: e.message });
    }
    // result.results = model.slice(startIndex, endIndex);
  };
}