module.exports = {
	// api_domain : "https://learndevelopment.herokuapp.com" // <-- host of the backend that the app uses to make requests
	// api_domain : "http://localhost:8000" // <-- host of the backend that the app uses to make requests
	api_domain : "https://learndev.onrender.com" // <-- host of the backend that the app uses to make requests
}

// https://learndev-backend.herokuapp.com/graphql
